{% extends 'base.html.twig' %}

{% block title %}miditra{% endblock %}

{% block body %}
<div class="container mt-15 mb-5">
    <div class="row justify-content-center">
        <div class="col-md-6">

            {% for flashMessage in app.flashes('success') %}
            <div class="alert alert-info" role="alert">{{ flashMessage }}</div>
            {% endfor %}
            
            <form method="post">
                {% if error %}
                    <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}
            
                {% if app.user %}
                    <div class="mb-3">
                        You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
                    </div>
                {% endif %}
            
            
                <h1 class="h3 mb-3 font-weight-normal text-center">Hiditra</h1>
                <label for="inputEmail">Mailaka</label>
                <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
                <label for="inputPassword" class="mt-2">Teny miafina</label>
                <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
            
                <input type="hidden" name="_csrf_token"
                       value="{{ csrf_token('authenticate') }}"
                >
            
                {#
                    Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                    See https://symfony.com/doc/current/security/remember_me.html
            
                    <div class="checkbox mb-3">
                        <label>
                            <input type="checkbox" name="_remember_me"> Remember me
                        </label>
                    </div>
                #}
                <div class="d-grid gap-2">
                    <button class="btn btn-info mt-2 btn-block" type="submit">
                        Miditra
                    </button>
                    <hr>
                        <p class="text-center">tsy manana kaonty ianao? Manorata anarana <a href="{{ path ('app_register')}}">eto</a> </p> 
                </div>

            </form>    
        </div>
    </div>
   
</div>

{% endblock %}
